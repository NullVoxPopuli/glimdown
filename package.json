{
  "name": "glimdown-monorepo-root",
  "version": "0.0.0",
  "description": "Glimmer + Markdown",
  "repository": "git@github.com:NullVoxPopuli/glimdown.git",
  "author": "NullVoxPopuli",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "turbo run test --output-logs errors-only",
    "lint:fix": "turbo run lint:fix --output-logs errors-only",
    "lint": "turbo run lint --output-logs errors-only"
  },
  "devDependencies": {
    "@glimdown/dev": "workspace:*",
    "concurrently": "^8.0.1",
    "turbo": "1.9.3"
  },
  "volta": {
    "node": "18.16.0",
    "pnpm": "8.2.0"
  }
}
